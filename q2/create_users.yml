---
- hosts: test
  become: true
  remote_user: rae
  vars_files: 
    - user_list.yml

  tasks:  
    - name: create users
      loop: "{{ developer }}"
      ansible.builtin.user: 
        name: "{{ item }}"
        state: present

