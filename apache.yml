---
- name: install a web server
  hosts: web
  become: true

  tasks: 
    - name: install httpd
      ansible.builtin.dnf:
        name: httpd
        state: latest

    - name: start and enable the service
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true


